apiVersion: v1
kind: Service
metadata:
  name: tyfyihb
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tyfyihb"
    port: 8080
  selector:
    app: tyfyihb
